const fs = require('fs')

function reset(interaction, path){
    /*fs.rmSync(path)
    interaction.reply('Reset the config')*/
    interaction.reply('a')
    interaction.reply('b')
}
module.exports = {reset}